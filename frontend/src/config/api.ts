// Unified backend base URL for API calls
// Update this URL when backend consolidation is finished and mock arrays are removed
export const API_BASE_URL = import.meta.env.VITE_API_BASE_URL || 'http://localhost:5000/api';